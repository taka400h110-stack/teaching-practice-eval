diff --git a/.github/workflows/ci-ui-audit.yml b/.github/workflows/ci-ui-audit.yml
index 0a3e7ca..da4560b 100644
--- a/.github/workflows/ci-ui-audit.yml
+++ b/.github/workflows/ci-ui-audit.yml
@@ -239,6 +239,8 @@ jobs:
           - stats-malformed
           - mobile
           - provider-health
+          - a11y
+          - keyboard
     steps:
       - name: Checkout
         uses: actions/checkout@v4
@@ -269,6 +271,10 @@ jobs:
             npm run test:e2e:optional:mobile
           elif [ "${{ matrix.target }}" = "provider-health" ]; then
             npm run test:e2e:optional:provider-health
+          elif [ "${{ matrix.target }}" = "a11y" ]; then
+            npm run test:e2e:optional:a11y
+          elif [ "${{ matrix.target }}" = "keyboard" ]; then
+            npm run test:e2e:optional:keyboard
           else
             echo "Unknown matrix target: ${{ matrix.target }}"
             exit 1
@@ -305,6 +311,8 @@ jobs:
           - stats-malformed
           - mobile
           - provider-health
+          - a11y
+          - keyboard
     steps:
       - name: Checkout
         uses: actions/checkout@v4
@@ -335,6 +343,10 @@ jobs:
             npm run test:e2e:optional:mobile
           elif [ "${{ matrix.target }}" = "provider-health" ]; then
             npm run test:e2e:optional:provider-health
+          elif [ "${{ matrix.target }}" = "a11y" ]; then
+            npm run test:e2e:optional:a11y
+          elif [ "${{ matrix.target }}" = "keyboard" ]; then
+            npm run test:e2e:optional:keyboard
           else
             echo "Unknown matrix target: ${{ matrix.target }}"
             exit 1
